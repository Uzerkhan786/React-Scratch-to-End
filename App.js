//Using CDN Links we create the html element and display it 
const header = React.createElement('h1', {}, 'Hello React using CDN Links');
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(header)

